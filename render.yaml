version: 2
services:
  - type: web
    name: notesaura-ai
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: SARVAM_API_KEY
        sync: false
    healthCheckPath: /health
